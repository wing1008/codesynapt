# insp-004 클린확정 기록 (2026-06-12, wf_81300135-829)

재점검(에이전트 재배포)에서 **제외**되는 항목/체크. 발견자+반박자 체인을 통과한 PASS만 기록.
항목 단위 클린: **burn-spots**(과거 데인 곳 6종 재발 0), **blockers**(보안·오프라인·404·라이선스 0건).
⚠️ 비평자 단서: blockers 체크리스트는 0.0.4 diff 기반 재도출 필요, perf/docs 카나리아는 가짜(클린 신뢰 할인) — 이 둘은 클린이어도 재배포에 사각 검증으로 포함.

## blockers
캐너리: Proved each harness check can fail before trusting its PASS. (1) Binding check: started a deliberately bad node server bound to 0.0.0.0:17777 — netstat reported `TCP 0.0.0.0:17777 ... LISTENING`, confirming my netstat parse distinguishes a wildcard bind from CodeSynapt's 127.0.0.1 (so a real 0.0.0.0
- [CLEAN] (a) Bind host = loopback only. control-server.cjs:2306 `startControlServer(port, host='127.0.0.1')` and :2325 `server.listen(port, host,...)`. Live netstat on my port 17321: `TCP 127.0.0.1:17321 0.0.0.0:0 LISTENING` — bound to 127.0.0.1, NOT 0.0.0.0.
- [CLEAN] (a) CORS never leaks to cross-origin pages. evil Origin (https://evil.com) GET /summary returns 200 with NO Access-Control-Allow-Origin header (grep count 0); loopback Origin (http://localhost:3000) gets ACAO reflected (count 1). Code path control-server.cjs:194-203 allowedOrigin() reflects only loopback hostnames, never `*`.
- [CLEAN] (a) DNS-rebinding defense active. `curl -H 'Host: attacker.com'` -> `{"error":"forbidden host: attacker.com"}` (403). control-server.cjs:1492-1496 validates Host header against loopback names.
- [CLEAN] (a) Auth gate present but optional by design: control-server.cjs:1563-1570 enforces Bearer token only when CS_AUTH_TOKEN configured; with no token the loopback-only bind + Host/CORS gates are the boundary (matches product model).
- [CLEAN] (b) Offline: no outbound external http(s) in shipped lib/bin. Grep for https?.get/request, fetch, axios, telemetry, sentry, dns over packages/core: all http.get/http.request targets use HOST='127.0.0.1' (codesynapt.cjs:61, codesynapt-mcp.cjs:29); parser.js hits are pattern-detection regexes, not calls. Scan ran fully on a private scratch project with no dependency on network and returned a valid graph (/summary fileCount 3).
- [CLEAN] (b) Desktop auto-updater is opt-in, defaults fully offline. electron/main.cjs:2899-2908 setupAutoUpdater() returns early unless CS_ENABLE_UPDATER==='1' — no require('electron-updater'), no feed resolution, no network by default; checkForUpdatesAndNotify (:2947) only reached when explicitly opted in.
- [CLEAN] (c) Robustness: unknown route -> 404 `{"error":"unknown endpoint","path":"/zzz/unknown"}`; malformed %-encoding -> 400 `{"error":"malformed URI escape in path"}`; garbage query (depth=zzz) -> 200 graceful degrade (depth:null), no crash; oversized URL id (32KB) -> 431; path traversal /file/..%2f.. -> 400 `{"error":"outside root"}`; garbage POST JSON -> 400 `{"error":"invalid JSON body"}`; 20MB POST body -> 400 usage error. Server /health still ok after all. No stack traces or absolute paths leaked (grep for stack/node:internal/F:\ on error bodies = NO STACK LEAK). control-server.cjs:2298 error handler emits only e.message.
- [CLEAN] (d) License: npx license-checker --production summary = MIT 87, Apache-2.0 15, ISC 13, BSD-3-Clause 12, Python-2.0 1, AGPL-3.0-or-later 1 (own pkg), (MIT OR WTFPL) 1, BlueOak-1.0.0 1, Unlicense 1 — all permissive/AGPL-compatible. Targeted grep for UNKNOWN/UNLICENSED/GPL-2/SSPL/proprietary returned nothing.
- [CLEAN] (d) Shipped file list (npm pack --dry-run) = 78 files, 3.0MB unpacked, includes electron/main.cjs, packages/core/bin+lib, server.js; no stray node_modules updater bundled (hasUpdater=false). Shipped package is root `codesynapt` v0.0.4, license AGPL-3.0-or-later.
- [CLEAN] Cleanup verified: my server (port 17321) stopped — netstat shows only TIME_WAIT client sockets, no LISTENING; canary 0.0.0.0 server (17777) gone; ~/.codesynapt/port restored to original 7799; my daemon registry entry removed; repo `git status` shows no tracked changes (only pre-existing untracked build/ icons that were present at session start).

## burn-spots
캐너리: Five independent canaries, all caught: (1) appended fake 'npm notice 1.2kB .codesynapt/observed-edges.jsonl' and '… registry.test.cjs' lines to a copy of the pack listing — leakage grep matched both (exit=0), so the real exit=1 PASS is meaningful; (2) CRLF-ified a copy of public/index.html — CR-byte
- [CLEAN] (1) Tarball leakage PASS — `npm pack --dry-run` at repo a937e97 (codesynapt-0.0.4.tgz, 78 files): grep -iE '\.codesynapt|\.test\.|observed-edges|recall-suspect|competitor-watch' over the full listing returned exit=1 (zero matches), while the excluded artifacts DO exist on disk (./.codesynapt/observed-edges.jsonl, ./.codesynapt/recall-suspects.jsonl, packages/core/lib/registry.test.cjs) so the exclusions are non-vacuous; registry.test.cjs is the only test file under packaged dirs and matches the !**/*.test.cjs negation.
- [CLEAN] (2) CRLF/CSP PASS — .gitattributes has blanket `* text=auto eol=lf` with explicit comment about public/index.html CSP importmap; `git check-attr` shows public/index.html attr eol=lf, `git ls-files --eol` shows i/lf w/lf, working-tree CR-byte count = 0 of 34557 bytes; independently recomputed sha256 of the inline importmap = sha256-NfHJr+xcDJiAGqHYgFEaR5vX+0YDa2Tbke39iP8L6uY= and confirmed it is present in the CSP meta (in CSP: true).
- [CLEAN] (3) Ghost desktop PASS (code-read per task spec) — electron/main.cjs:2542-2569 registerDesktopDaemon contains the GHOST-TWIN guard (2026-06-11 incident comment) at lines 2553-2561: readDaemon(phash, 60000); fires when existing.desktop && existing.pid !== process.pid && existing.epoch !== DESKTOP_EPOCH; emits console.warn '[cs] another desktop already serves this project (pid …, port …)', emitTrace('issue', …), and a renderer error toast. Reachable: called from main.cjs:265 (project load) and main.cjs:2597 (control-server listen callback), both on the normal startup path; DESKTOP_EPOCH is a per-process randomUUID so a genuine second desktop always mismatches both pid and epoch, while self-re-registration matches both and stays silent.
- [CLEAN] (4) Zombie daemons PASS — code: registry.cjs:57-73 writeAtomic does rename, one retry, then PLAIN-overwrite fallback with tmp-unlink in finally (EPERM rationale comment at :46-56); codesynapt.cjs:507-533 heartbeat uses SEPARATE try blocks so a touch() throw can no longer starve the reap (17-daemon incident comment), with inFlight guard (:524, fed by control-server.cjs:2304/2321-2323 per-request counter) and 30s lastRequestAt activity guard (:527). Live x2: with the lease file held open by a second process (FileShare ReadWrite, no Delete), raw renameSync onto it failed EPERM (scenario proven real) yet registry.touch() did not throw, file content updated to the new payload via fallback, zero .tmp litter — identical results on both runs. Live idle-reap: all 4 `cs serve` daemons I started self-exited with '[cs] no live sessions/viewers — daemon self-exit' once idle (>20s grace + 3 empty ticks + 30s activity window), and ports 17311-17314 show no listeners afterward; daemons did NOT reap mid-trace because trace run holds a session lease (codesynapt.cjs:2503-2521).
- [CLEAN] (5) CLI cross-project misrouting PASS — live with two scratch projects under isolated CS_HOME+USERPROFILE (F:\tmp\insp_regress\home; real ~/.codesynapt and the user's :7707 desktop untouched). Misroute path: only projB's daemon (port 17311) running, legacy port lock = 17311; `cs summary` from projA printed the loud warning twice (2 runs): '⚠️ codesynapt: daemon on :17311 is serving a DIFFERENT project — results are NOT for your cwd. / daemon root: F:\tmp\insp_regress\projB / your project: F:\tmp\insp_regress\projA / Fix: run `cs ensure` …'. Control: same command from projB printed no warning. Registry routing: after starting projA's daemon on 17312, the lock still pointed at 17311 (daemon logged 'port lock kept at 17311') yet `cs summary` from projA returned root F:\tmp\insp_regress\projA with no warning — per-project registry entry preferred over the global lock exactly as resolvePort() (codesynapt.cjs:43-59) claims.
- [CLEAN] (6) cpu-prof-dir quoting PASS — code at codesynapt.cjs:2710-2712: forward-slash normalization + CONDITIONAL quoting (quote only when /\s/ matches, with comment that unconditional quoting was itself a regression). Live, 2 runs EACH: (a) no-space project F:/tmp/insp_regress/projT and (b) space project 'F:/tmp/insp burn sp/proj' — all four `cs trace run -- node main.cjs` invocations produced '1 profile(s) collected', '4 raw frame-edges', a full report (observed call edges: 3 — 1 confirm static, 2 NEW dynamic: main.cjs#<module>@1 → lib.cjs#work@2 / lib.cjs#fib@1), and a saved .codesynapt/traces/runtime-*.json. Profile collection succeeded identically in the spaced path.

## diff-review
캐너리: Three positive/negative controls before trusting any verdict. (1) Tracer harness: ran the identical pytracer command on app_plain.py (no sys.exit) FIRST — out file appeared with 1 valid pair ({\"cf\":\"app_plain.py\",\"cl\":4,...}), proving 'out file MISSING' in the sys.exit case is a real data loss
- [CLEAN] Working tree == audited commit: `git rev-parse HEAD` = a937e972... and `git diff HEAD -- <all audited files>` is empty — every citation is from CURRENT code, and all module probes used fresh `node -e` processes requiring repo paths.
- [CLEAN] Desktop async flow promise cannot hang/crash the server: the `await r` at electron/main.cjs:2010 sits inside handleControlRequest's function-level try (main.cjs:1533) whose catch (main.cjs:2504-2505) writes a 500.
- [CLEAN] Headless flow promise rejection handled: control-server.cjs returns r.then(...).catch(e => writeJson(res,500,...)) with res.writableEnded guard (control-server.cjs ~1808-1810).
- [CLEAN] Signature baseline IS reset on project switch: `_prevSigMap = null` at electron/main.cjs:271 inside startScanner (alongside `_prevDeadSet = null` at :270) — no cross-project false signature alerts.
- [CLEAN] E2E python tracing x observe pipeline works on the happy path: fresh `cs serve` (port 18871, scratch project) + `cs trace run -- python app_plain.py` → '1 confirm a static call ... merged into live graph: 1 observed edge — persisted'.
- [CLEAN] E2E expression flow on headless server: `cs symbol flow app.py#work@3` → correct facts (params: n; L4 assist(param:n); return ← call:assist); E2 on a Python symbol returns the honest JS-family scope note instead of guessing; ambiguous short name returns a candidate list, not a wrong pick.
- [CLEAN] Desktop /health parity fields present (epoch/graphVersion/traceVersion) at main.cjs:1571-1573; headless /health verified live returning the same keys.
- [CLEAN] symbolAtLine <module>-fallback change covered by new test (tests/runtime-trace.test.js) and full suite green: 31 files, 273/273 passed (vitest run, 0.0.4 tree).
- [CLEAN] profDir leftovers on early-exit paths self-heal: >6h prof-* sweeper at codesynapt.cjs:2654-2662 runs on every trace invocation.
- [CLEAN] Registry hygiene: my scratch `cs serve` daemons/*.json entry was removed on daemon self-exit (verified file gone); desktop heartbeat interval is unref'd (main.cjs registerDesktopDaemon); ghost-twin warn (main.cjs:2549-2561) correctly excludes self via pid+epoch check.
- [CLEAN] readFile wired to /symbol/flow is root-scoped on BOTH servers (main.cjs:2002-2006 resolve+startsWith guard; control-server.cjs isInsideRoot) and read errors are caught in symbol-views (try around ctx.readFile → 500, not crash).
- [CLEAN] Repo left untouched: `git status --porcelain | grep -v '^??'` → NO TRACKED CHANGES (only pre-existing untracked build/_* icon scratch from before this audit); no processes of mine left (ports 17235/18871 clear, both serve daemons self-exited); user's daemons on 7707/17234/18742 untouched.

## docs
캐너리: Two deliberate-failure probes: (1) 'cs nonexistentcmd' → exit 1 + 'error: unknown command: nonexistentcmd' — confirms my harness surfaces CLI failures (I capture exit codes directly after discovering my first ci-gate check was pipe-masked by 'head' returning 0; re-ran unpiped and got the true BREACH
- [CLEAN] Version strings: CLI package version 0.0.4 matches release prep (node -p require('package.json').version → 'codesynapt 0.0.4'); CHANGELOG 0.0.4 section present and dated
- [CLEAN] 11 cs_* MCP tools claim (README:18,40, CLAUDE.md, docs/mcp-setup.md): live JSON-RPC tools/list over stdio returned exactly 11: cs_summary, cs_query, cs_blast, cs_intent, cs_health, cs_change, cs_trace, cs_ui, cs_symbol_summary, cs_symbol_search, cs_symbol_explore
- [CLEAN] Test suite green at a937e97: npx vitest run → 'Test Files 31 passed (31) / Tests 273 passed (273)' (count contradicts only the README badge — see finding)
- [CLEAN] Headless commands: cs scan --summary (6 files/2 edges), scan --json (valid JSON, nodes+edges), cs serve (health ok), cs ci-diff main..HEAD (markdown impact table), cs ci-gate (exit 0 within limits, exit 1 on breach — verified both ways without pipe-masking)
- [CLEAN] Connected file-level commands all work against a daemon: health/summary/ls/show/read/refresh/deps/users/orphans/find/search(1ms warm)/blast/safety(🟢+advice)/bundle(budget respected)/suggest/preflight(warn surfaced)/env/secrets/feature/url(/billing matched correctly under PowerShell; the Git-Bash '/billing'→'C:/Program Files/Git/billing' mangling was my harness artifact)/schema/legacy/packages/package-graph/timeline/tour/changes/diff(honest 'no change recorded')/history/vendors/context(correct root on clean daemon)/bench/trace + trace stats/sessions/export/clear
- [CLEAN] Symbol layer on headless daemon: symbol summary (19 symbols/10 edges, 100% coverage), accounting ('total 19 = entries 14 + reachable 5 + possible 0 + dead 0 (unexplained: 0)' with static-floor caveat exactly as claimed), find, callers, callees, blast, node (full id from find resolves — initial 'symbol not found' was a hijacked-daemon artifact, re-verified clean)
- [CLEAN] 0.0.4 headline — Python runtime tracing: 'cs trace run -- python src/calc.py' → '4 raw frame-edges ... 3 confirm a static call · 1 NEW (dynamic)' including the exact claimed dynamic case: 'src/calc.py#dyn@7 → src/calc.py#helper@1' from globals()[name](); 'survives a crashing script' verified: exit=1 run still produced 2 observed edges; merge persistence message and amber-merge wording all present
- [CLEAN] 0.0.4 headline — expression layer: 'cs symbol flow' returns flow facts for all four claimed languages (JS login: param→makeToken→verify→return chain; Python compute: flow + '1 flow(s) not trackable in E1 — counted, not guessed'; Java Greeter.greet; C# Worker.Run); param-level E2 'cs symbol flow login user' → 'argument blast — d1 makeToken(u)'; E2 on Python declines honestly: 'argument-level blast (E2) is JS-family for now' — exactly matching CHANGELOG '(JS family in this release)'
- [CLEAN] Node V8 tracing (0.0.3 claim): CPU-busy script → '1 raw frame-edges, 1 confirm a static call' (spin→helperC); zero-sample quick script reports honest zeros, nothing fabricated
- [CLEAN] Lua bare-call fix (0.0.4) holds in a single-language project: fresh lua-only build → 3 symbols, 2 edges, main_lua listed as caller of helper_lua (breaks only when Python coexists — see HIGH finding)
- [CLEAN] Live symbol map (always-on) verified for JS, Python, C#: newly added files' symbols + call edges appear in the running daemon within ~5-10 s without restart
- [CLEAN] /health epoch/graphVersion/traceVersion (0.0.4 claim): present in headless /health response; desktop counterpart present in code at electron/main.cjs:1575; duplicate-desktop warning code present at electron/main.cjs:2558
- [CLEAN] cs write/cs edit: gated off without CS_AUTH_TOKEN with a clear actionable error ('write disabled: start the server with CS_AUTH_TOKEN'), and both work when the token is set (edit '--unique find' replacement verified via cs read)
- [CLEAN] cs init (with server up, sandboxed HOME/USERPROFILE): wrote CLAUDE.md + ~/.claude/commands/codesynapt.md + codesynapt-auto.md + printed the exact 'claude mcp add' line — matches README/help
- [CLEAN] Current-code discipline: every command executed as 'node F:/MyProg/3dgit/filegraph3d/packages/core/bin/codesynapt.cjs ...' (repo path, fresh node process per invocation); no global install used; repo left untouched (git status: 0 modified tracked files; only pre-existing untracked build/ icon artifacts)

## dogfood
캐너리: Known-answer fixture harness (F:/tmp/cs-audit-0004/recursion-test2.mjs) built on the repo's real parser+graph: fixture has exactly 2 known edges-of-interest. The harness asserts (1) a deliberately WRONG edge 'rec -> caller' — it correctly reported false (a tool claiming it would have been caught), a
- [CLEAN] (a) symbol accounting: total 1486 = entries 243 + reachable 675 + possible 225 + dead 343, unexplained: 0 — arithmetic verified by hand, reproduced on two fresh servers; dead list carries the floor caveat and dynamic-site count (411) inline
- [CLEAN] (b) extractFlowAuto callers/callees vs grep: callers handleSymbolView (symbol-views.cjs:211 call, inside @137) + flow-multilang.test module — exactly the grep set; callees extractFlow + extractFlowTS match source lines 237/241; export-object reference correctly surfaced as 'referenced as a value ... NOT dead'
- [CLEAN] (b) appendObservedBatch: both real callers found in the right files/functions (electron/main.cjs:2074 in handleControlRequest@1505; control-server.cjs:1743 in handleControlRequest@1483); test-file call surfaced as candidate (counted, hedged); callees observedFileFor + loadValidObservedPairs match source
- [CLEAN] (b) registerDesktopDaemon: callers startScanner (call at main.cjs:265) + startControlServer (call at 2597) exactly match grep; callees projectHash/readDaemon/emitTrace/local touch@2563 all verified in source 2542-2569 (the one missing callee, _deskRegistry.remove, is finding #3)
- [CLEAN] (b) signatureDelta: callers handleControlRequest (main.cjs:1957) + flow-sig.test module = exact grep set; callee collectSignatures verified at source line 215; g.callersOf correctly surfaced as candidate not asserted
- [CLEAN] (b) observeRuntimeEdges: all four real call locations covered (main.cjs:1922+2072 precise, scanner.js:730 in Scanner.buildSymbolGraph@705 precise, control-server.cjs:1738 as counted candidate, test module precise); callees symbolAtLine + addEdge match method body — zero wrong-file callers across all 5 audited functions
- [CLEAN] (c) blast hop-1 on symbol-views.cjs = {electron/main.cjs, control-server.cjs, surfaces.test.js} — exactly matches `grep -rln symbol-views` (3/3, no extras, no misses); hop-2/3 spot-checked sane; vendored dist/win-unpacked copies correctly excluded from the graph; API-level caveat content itself is accurate (4 dynamicFiles verified to contain require(expr)/import(expr))
- [CLEAN] (d) flow rel_of (Python, pytracer.py:39): all 12 facts hand-verified against source — every line number (L40 get, L44 startswith, L49 isabs+abspath, L50 replace, L51 startswith, L54 len, returns L42/46/53/56) and every provenance tag (param:filename vs literal vs unknown) is true; closure var root_norm honestly 'unknown'; 6 untrackable flows counted not guessed; reproduced 2x
- [CLEAN] (d) flow signatureDelta (JS): 9 facts all true (collectSignatures(param:g) L215, values() L216, get(unknown) L219/220, push L222, slice(literal,literal)x2 L225, callersOf(unknown) L226, return L230); 7 unresolved counted
- [CLEAN] (d) flow appendObservedBatch (JS): 18 facts all true including call-provenance chaining (existsSync(call:observedFileFor), renameSync(unknown, call:observedFileFor), statSync(call:join)); all 3 returns (L222 literal, L257 unknown, L258 literal) correct; 15 unresolved counted
- [CLEAN] (e) no duplicate symbol nodes: 0 duplicate ids and 0 duplicate file+name+line across 1486 symbols in /symbol/graph; no wrong-file callers found anywhere in the audit; ambiguous symbol-name resolution (registerDesktopDaemon find returned 2 matches) honestly lists candidates instead of guessing
- [CLEAN] Fresh-code discipline: every server was `node packages/core/bin/codesynapt.cjs serve` from the repo working tree at a937e97 on my own pinned port 17234 (CS_PORT pinned past registry routing); fixture scripts require() repo lib paths directly; user's 7707 untouched (was down); other agents' backends on 18742/17473 left alone; git status byte-identical before/after (diff = GIT_UNCHANGED)

## e2-blast
캐너리: Two canaries before trusting any empty-impacted PASS: (1) positive-detection canary — fx3 chain where the flow is REAL: blast f1.p1 must produce impacted entries; it did (f2:d1..f5:d4), proving the serve->CLI->JSON pipeline actually surfaces impacted hits, so fx1/fx2/fx5/fx8 empty-impacted results a
- [CLEAN] Ambiguity STOPS the walk and is counted, not guessed (method-name with >=2 impls): fx1 `shape.scale(factor)` with Circle.scale+Square.scale -> L2 declines (3 candidateCallees, 0 confident), blast resize.factor -> impacted:[], unresolvedTargets:1 (reproduced twice)
- [CLEAN] Param-shadowing callback produces NO false edge: fx2 `function apply(formatRecord, secret){ return formatRecord(secret) }` with imported helpers.formatRecord -> L2 callees:[], dynamicSites:1, blast apply.secret -> impacted:[], unresolvedTargets:1
- [CLEAN] Same-name function in two imported files -> declined and counted: fx5 route(payload) calling `digest` (declared in both a.js and b.js) -> impacted:[], unresolvedTargets:1, never picks one (reproduced twice)
- [CLEAN] Renamed barrel re-export not guessed: fx5 pipeline.item calling wrap.transform (impl realTransform behind `module.exports={transform: realTransform}`) -> impacted:[], unresolvedTargets:1 (recall loss only, no wrong edge)
- [CLEAN] Local-rebinding shadow inside a block (fx8 `const fmt = cb; fmt(secret)` next to a real `fmt('banner')` call) -> L2 records no confident callee, blast secret -> impacted:[], unresolvedTargets:1 — the latent E2 name-join (symbol-flow.cjs:170-171 joins call sites to callees by NAME, not site) did not fire falsely in any fixture where L2 itself was correct
- [CLEAN] Recursion terminates via visited set: fx4 selfRec(n) -> counted unresolved (no L2 self-edge), mutual ping/pong -> d1 pong(v), d2 ping(v) then stop, no hang (20s timeout never hit)
- [CLEAN] 100-hit cap is REPORTED: fx7 120-way fan-out -> truncated:true in JSON and '(truncated at 100 hits)' in CLI render
- [CLEAN] returnsParam correct both ways: fx7 hub returns p -> returnsParam:true; fx3 f1 returns f2(p1) (call result, not the param) -> returnsParam:false
- [CLEAN] argIndex mapping correct with mixed literal/param args: fx5 caller(x) calling withDefaults(1, x) -> argIndex:1 lands on the second parameter slot
- [CLEAN] Exercised CURRENT repo code: every run invoked node directly on F:/MyProg/3dgit/filegraph3d/packages/core/bin/codesynapt.cjs (commit a937e97) with fresh `cs serve` processes per fixture on ports 17431-17439; repo tracked files unchanged at end (git status --porcelain shows no tracked modifications), all my servers killed, my stale registry entry (daemons/d480545bc68a27b2.json -> fx1) removed, user's 7707 untouched

## flow-real
캐너리: Two deliberate breaks, both caught (F:/tmp/csflow_audit/canary.cjs): (A) fed ms.parse with startLine 47 (off by one) — tool returned {params:[], calls:0, returns:0}, which the comparator flags as all-facts-missing FAIL, proving a silently-empty result cannot masquerade as PASS; (B) corrupted one tru
- [CLEAN] JS extractFlow on real npm code ms/index.js parse (line 48): all 4 calls (String/exec/parseFloat/toLowerCase), all 10 returns incl. call:parseFloat@99 and bare `return;`→literal, unresolvedFlows=8 — exact match to hand-derived truth (PASS js1).
- [CLEAN] JS ms/index.js fmtLong (line 138): module-scope vars d/h/m/s correctly unknown+counted (uF=5), plural args [param:ms, call:abs, unknown, literal] and returns call:plural exact (PASS js2).
- [CLEAN] JS ansi-align/index.js ansiAlign (line 5): early returns param:text correct, destructured/conditional/logical inits all unknown AND counted (uF=9), nested map-callback flows correctly excluded (PASS js3).
- [CLEAN] JS wrap-ansi exec: all 29 call facts incl. chained `new RegExp(...).exec(pre.slice(i).join(''))` (exec arg=call:join, RegExp arg=literal, slice arg=unknown+counted) and '(pattern)' marker for `options = {}` default param — exact match except the two reported defects (calls/params clean).
- [CLEAN] Python posixpath.join: params [a,p] incl. *p splat name, sep=call:_get_sep chained into startswith/endswith args, *p splat arg unknown+counted — correct apart from reported defects.
- [CLEAN] Python bisect.insort_right: params [a,x,lo,hi,key] across defaults and keyword-only marker, keyword arg key=key unknown+counted (uF=1), nested key(x)→param:x — correct apart from insert arg0 defect.
- [CLEAN] Java: 6/9 methods exact match (addItem, normalize, bump, decorate, wrap, findFirst, headroom) — call-result chains (call:toLowerCase return), literal returns, binary-expression returns unknown+counted, loop-var returns unknown+counted, assignment_expression chains (`assembled = assembled.concat(close)`) tracked correctly.
- [CLEAN] C#: 6/8 methods exact match (Add, Normalize, Bump, Decorate, Wrap, FindFirst) — equals_value_clause unwrap, argument-node unwrap, static member call `string.Concat` name resolution, boolean/null literals, C# constructor recorded with param:sep arg.
- [CLEAN] extractFlowAuto language routing: js/py/java/cs all routed to a working walker (no 'unsupported' on any of the 23 cases); tree-sitter wasm grammars present (tree-sitter-python/java/c_sharp.wasm confirmed in node_modules/tree-sitter-wasms/out).
- [CLEAN] Current-code discipline: every run required packages/core/lib/symbol-flow.cjs by absolute repo path in a fresh node process (no installed copy, no server); repo at a937e97 with zero tracked modifications after the audit (git status --porcelain | grep -v '^??' → empty).

## journey
캐너리: Two proofs. (1) Truth-diff harness: after the file-graph check PASSed, I corrupted the expected-edge set (replaced real edge py/animals/dog.py->py/animals/base.py with fabricated src/utils.js->src/index.js) and re-ran the same diff — it printed 'CANARY(corrupted truth): FAIL missing: [src/utils.js->
- [CLEAN] Install journey: npm pack (78 files) + npm install of the tarball into a clean consumer succeeded; postinstall copy-vendor ran; cs/.bin shims work — proof: `cs --help` and `cs scan` ran from consumer/node_modules/.bin
- [CLEAN] File graph EXACT: 10/10 edges match the imports I wrote (7 JS + 3 Py), 0 phantom edges, 13/13 nodes — proof: scripted set-diff of scan --json vs hand truth printed 'TRUTH: PASS missing: [] phantom: []'
- [CLEAN] scan --summary numbers correct (files: 13, edges: 10, ext mix .js 7/.py 5/.json 1); orphans=2 are package.json + animals/__init__.py (defensible)
- [CLEAN] JS symbol callers/callees match hand truth: formatArea callers=[runApp] callees=[round2]; runApp callees = Circle, Square, formatArea, Circle.area, Square.area, dispatch (console.log correctly declined as builtin: '3 declined = 3 stdlib/builtin')
- [CLEAN] Callback ledger works: `cs symbol callers onStart` → '(none)' plus 'referenced as a value by (passed as callback/arg — used, NOT dead): src/dynamic.js#<module>' — no silent drop of the handlers-table reference
- [CLEAN] `cs symbol blast dispatch` shows the dynamic caveat ('⚠ Static call graph — a floor ... impact can expand through those') and correct 2-symbol impact (runApp, index <module>); file-level `cs blast src/dynamic.js` hops correct (dynamic.js → app.js → index.js)
- [CLEAN] Expression flow (Layer 3) correct on all 3 JS functions tested: formatArea 'L6 round2(param:x)'; runApp Circle(literal)/Square(literal)/formatArea(call:area)/dispatch(literal)/return←literal; untrackable flows explicitly counted: '1 flow(s) not trackable in E1 (object/closure/mutation) — counted, not guessed'
- [CLEAN] Accounting arithmetic: unexplained=0 (total 23 = entries/reachable/possible/dead partition closes; also 0 on repo build with 35 symbols) — the bar 'unexplained MUST be 0' holds arithmetically (but see HIGH findings for label correctness and the silently shrunken denominator)
- [CLEAN] JS `cs trace run -- node src/index.js`: program output identical to plain run (area: 12.57 / area: 9 / started), exit code propagated, observed edge merged + persisted ('persisted (survives rebuilds until the involved files change)'), honest coverage caveat printed
- [CLEAN] Hygiene: both my servers (17471 packed, 18233 repo) are gone, registry entry C:\Users\USER\.codesynapt\daemons\f89760654e8ed039.json self-removed ('registry entry gone (clean)'), user's port 7707/17321 backends untouched, scratch confined to F:/tmp/cs-audit-004, `git status --porcelain` shows zero tracked-file changes at a937e97

## mcp
캐너리: Two-part proof the harness can fail. (1) Framing validator: ran the same driver loop against a fake MCP server that replies with 'GARBAGE NOT JSON' and {\"jsonrpc\":\"1.0\",\"id\":1,\"bogus\":true} — validator flagged all three injected defects: UNPARSEABLE line, BAD jsonrpc, result-XOR-error violat
- [CLEAN] JSON-RPC framing valid for all 14 exchanges in both runs: every stdout line parsed as JSON, jsonrpc:'2.0', id matched request, result XOR error held — FRAMING_ERRORS: [] (driver validates every line; see canary)
- [CLEAN] initialize handshake correct: protocolVersion 2024-11-05, serverInfo {codesynapt 0.2.0}, capabilities.tools, instructions string present; notifications/initialized accepted silently; ping -> {} (37 bytes)
- [CLEAN] tools/list returns exactly the 11 documented cs_* tools with schemas (13,843 bytes): cs_summary, cs_query, cs_blast, cs_intent, cs_health, cs_change, cs_trace, cs_ui, cs_symbol_summary, cs_symbol_search, cs_symbol_explore
- [CLEAN] FILE-level data correctness PROVEN vs grep: cs_query{action:'users', id:'packages/core/lib/control-server.cjs'} returned exactly the 7 grep-truth importers (electron/main.cjs, scripts/benchmark.mjs, tests/control-server.test.js, tests/headless-features.test.js, tests/viewer-client.test.js, packages/core/bin/codesynapt-mcp.cjs, packages/core/bin/codesynapt.cjs) — 7/7 match, no extras, no misses
- [CLEAN] Response sizes all lean: largest tool response 4,108 bytes (cs_symbol_summary); summary 2.9 KB, callers 2.9 KB, safety 2.9 KB, intent 635 B — nothing near the 20 KB bloat bar; blast radius is compact-by-default with full=true opt-in
- [CLEAN] Clean errors for bad input: nonexistent file id -> {"error":"not found"} (106 B); unknown tool -> -32601 'unknown tool: cs_nope'; unknown method -> -32601; garbage stdin line ('this is not json {{{') silently ignored and server stayed alive (ping answered immediately after); no crash across all 14 calls in run1 + 6 in run2
- [CLEAN] Exercised CURRENT code: spawned F:/MyProg/3dgit/filegraph3d/packages/core/bin/codesynapt-mcp.cjs directly in fresh node processes (3 separate launches, fresh scan each — scannedAt timestamps current); stderr proves in-process backend scanned this repo on isolated ports 17432/17462; user's desktop port 7707 never contacted (USERPROFILE overridden so its port-lock was invisible, CS_PORT pinned to 17431/17461/17481)
- [CLEAN] cs_summary{project}: sane (fileCount 151, edgeCount 205, topHubs plausible, _guidance present); cs_blast{safety}: 'caution' with reasons incl. honest 'Dynamic import patterns — graph may be incomplete'; cs_intent{feature:'blast'}: honest via:'path' provenance; cs_symbol_summary: honesty counters present (unresolvedAmbiguous 6050, declineReasons breakdown, byEdgeKind incl. call-candidate 548 + observed 49)
- [CLEAN] Pure-attach mode exits 0 on stdin close (attach_test.cjs) — lifecycle defect confined to self-host fallback (reported above)
- [CLEAN] Hygiene: git status tracked-changes = 0 after audit (only pre-existing untracked build/_* icons); no leftover listeners on 17431/17432/17461/17481 (netstat: only one TIME_WAIT client socket); all servers I started were killed

## parity
캐너리: Two-sided: (1) comparator canary — copied the CLI flow JSON and corrupted unresolvedFlows 17→999, reran compare.py against the server output: it printed 'MISMATCH cli-broken vs srv / key=unresolvedFlows / 999 vs 17' and exited 1, proving the parity check is not vacuous; (2) wire canary — GET /symbol
- [CLEAN] Three-surface E1 parity, JS (symbolBlast): CLI (CS_PORT-pinned `cs symbol flow symbolBlast --json`), headless GET /symbol/flow?id=..., and direct require of symbol-views.cjs handleSymbolView produced byte-identical facts after stripping the server-only `meta` wrapper — comparator output: 'IDENTICAL across 3 surfaces: calls=19 returns=2 unresolvedFlows=17' (exit 0).
- [CLEAN] Three-surface E1 parity, Python (pytracer.py#rel_of@39, the NEW tree-sitter symbol-flow-ts path): 'IDENTICAL across 3 surfaces: calls=7 returns=4 unresolvedFlows=6' — and since this arrived as HTTP 200 with a full JSON body over the wire, the control server demonstrably resolves the flow thenable before writing (the known regression area).
- [CLEAN] Three-surface E2 parity (argument blast, symbolBlast/id): 'IDENTICAL across 3 surfaces' — impacted/unresolvedTargets=2/returnsParam/seed all match CLI vs server vs direct.
- [CLEAN] Thenable handling diff, desktop vs headless (code-read, desktop can't launch headlessly): main.cjs:2009-2012 `if (r && typeof r.then === 'function') { const rr = await r; return writeJson(...) }` with rejections caught by the handler-wide try/catch at main.cjs:2504 → 500; control-server.cjs:1806-1809 `r.then(...).catch(e => writeJson(res,500,{error:'flow failed: ...'}))`. Both await, both 500 on reject, both apply withMeta only on status 200. Parity OK (error message text differs slightly — cosmetic).
- [CLEAN] POST /symbol/observe (the past one-server-404 defect) present on BOTH servers: control-server.cjs:1715 and main.cjs:2062 — grep-verified.
- [CLEAN] 404 on unknown symbol id over the wire: GET /symbol/flow?id=no/such.js%23fn@1 → HTTP 404 {"error":"symbol not found"} (matches symbol-views.cjs:185).
- [CLEAN] E2 multilang honesty: `cs symbol flow rel_of filename --json` (Python + param) → scope note 'argument-level blast (E2) is JS-family for now' — declined with a visible note, not silence (symbol-views.cjs:200-201).
- [CLEAN] Current-code discipline: harness and direct scripts require() the repo paths directly (F:/MyProg/3dgit/filegraph3d/packages/core/...) in fresh node processes; CLI invoked as `node packages/core/bin/codesynapt.cjs` with CS_PORT=18267 pinned to MY harness (pinning bypasses registry routing per codesynapt.cjs:44/72), so no stale install/server and zero contact with the user's 7707 desktop.
- [CLEAN] Repo integrity after audit: `git status --porcelain | grep -cv '^??'` → 0 modified tracked files; HEAD still a937e97. Harness server (PID 516864, started 09:24 by me) killed; scratch confined to F:/tmp/cs-audit-flow/.

## perf
캐너리: Deleted the single line `import { v501 } from './f0501.js'` from synth/f0500.js and re-scanned with the same harness: it flagged 'canary edges: 2998 baseline: 2999 → CANARY CAUGHT: count harness flags mismatch' and the set-diff pinpointed exactly the removed edge 'f0500.js|import|f0501.js'. File res
- [CLEAN] (a) Full repo scan via CLI, twice — cold 1.067s, warm 1.026s (bar: 60s). Proof: PowerShell Measure-Command around `node packages/core/bin/codesynapt.cjs scan F:\MyProg\3dgit\filegraph3d --json` → 'scan1 (cold): 1.0668477 s / scan2 (warm): 1.0259835 s'; both outputs 151 files / 207 edges. Layer-1 scan has no on-disk cache (grep of scanner.js: only resolution caches in memory), so cold/warm differ only by OS file cache. Cross-check: a pristine copy with no .codesynapt scanned by `cs serve` also reported '151 files, 207 edges'.
- [CLEAN] (b) Determinism layer-1 — full SET diff (not just counts) of files+edges between the two scans: 'DETERMINISM(layer1 full set): IDENTICAL | files 151 edges 207'. Determinism layer-2 — three FRESH daemon processes (ports 18217, 18218, 18219/512MB-capped) each built the symbol graph from scratch; /symbol/summary identical all three times: symbolCount=1486, edgeCount=2637 (byEdgeKind call=1631, call-candidate=548, ref=431, type-ref=27), dynamicSiteCount=411 (in 171 symbols), unresolvedAmbiguous=6050, coverage 88/151 files (58%).
- [CLEAN] (c) Symbol flow on largest functions (top-4 by line-span heuristic over /symbol/graph: public/app.js#_allEvents@82, electron/main.cjs#readFile@2002, public/app.js#render@3487, packages/core/bin/codesynapt.cjs#resolveSymbol@1121) — all <2s, bar met: cold first call including lazy symbol-graph build 1.307s; warm calls 126–332ms (run twice each, e.g. render run1=1307ms/run2=150ms, readFile 135ms/130ms). Cap honesty verified against code, not just output: render returned calls=98, returns=1, unresolvedFlows=118, capped=false — looks contradictory vs the '200 facts' cap, but symbol-flow.cjs:111/118 counts ONLY calls+returns as facts (98+1=99 < MAX_FACTS=200); unresolvedFlows is a separate counted-not-guessed counter. capped flag is consistent and honest.
- [CLEAN] (d) Memory under `node --max-old-space-size=512` — no OOM anywhere: repo scan 0.955s exit 0 (151/207); synthetic 2000-file scan 1.046s exit 0 (2000/2999); full `cs serve` + symbol-graph build under 512MB heap succeeded with identical counts (1486/2637/411), /symbol/summary answered in 1.239s.
- [CLEAN] (e) Synthetic scale, 2000 JS files (chain i→i+1 = 1999 edges + 20 hubs × 50 fan-out = 1000 edges; known answer 2999) in F:/tmp/csaudit-perf/synth — scan1 1.682s, scan2 1.674s, no hang, counts EXACT: 'files 2000 edges 2999' both runs, full edge-set diff IDENTICAL. Known-answer fixture doubles as a false-verification anchor.
- [CLEAN] Current-code proof: every measurement invoked `node F:\MyProg\3dgit\filegraph3d\packages\core\bin\codesynapt.cjs` directly from the repo working tree (commit a937e97) in a fresh node process; daemons confirmed fresh via changing /health epoch per restart; never used a globally-installed `cs` or the user's desktop on 7707.
- [CLEAN] Hygiene: never touched port 7707 or the user's registry entry for this repo (symbol daemons served a scratch COPY → different projectHash; serve logs show 'port lock kept at 17321 (a live backend holds it)' so ~/.codesynapt/port was never clobbered by my instances). All 5 of my daemons confirmed down (self-exit observed in each serve log; /health connection refused). Final check: 'REPO TRACKED FILES UNCHANGED' (git diff --quiet && git diff --cached --quiet); remaining git-status noise is pre-existing untracked build/_* icon junk, not mine.

## sig-alerts
캐너리: Driver includes a deliberately wrong assertion: after a real param-add (function f(a) -> function f(a, b)) it asserts delta.length === 0. Output in BOTH runs: 'FAIL CANARY (must FAIL): asserting param-add produces 0 delta   delta=[{\"n\":\"f\",\"b\":\"function f(a)\",\"a\":\"function f(a, b)\"}]' — 
- [CLEAN] (a) param added: exactly 1 delta, correct name f, callers=1, before '(a)' / after '(a, b)' — PASS both runs (node F:/tmp/sigaudit/drive.cjs: 'PASS (a) ... delta=[{"n":"f","b":"function f(a)","a":"function f(a, b)"}]')
- [CLEAN] (b) param renamed: 1 delta for f, '(a)'->'(x)' — PASS both runs
- [CLEAN] (c) function renamed f->g: 0 delta, no wrong 'changed' claim (old key absent in cur, new key absent in prev; symbol-flow.cjs:220 skips undefined-before) — PASS both runs
- [CLEAN] (d) comment-only edit (leading + inside body): 0 delta — PASS; (e) body-only edit of brace-body function: 0 delta — PASS; (g) function moved 5 lines, text unchanged: 0 delta (file+qualifiedName keys are line-shift-proof per symbol-flow.cjs:190-192) — PASS
- [CLEAN] (f) two object-literal `click` methods sharing file+qualifiedName: SIG_COLLIDED sentinel excludes them entirely even when one changes — delta=[] PASS; transition f2 (unique key BECOMES collided -> skipped via curMap.get(key)===SIG_COLLIDED, symbol-flow.cjs:220) PASS; transition f3 (previously-collided key becomes unique -> skipped via before===SIG_COLLIDED) PASS
- [CLEAN] Desktop wiring — no alert on FIRST scan: `if (_prevSigMap)` guard at electron/main.cjs:1956; first build only sets the baseline at 1965
- [CLEAN] Desktop wiring — ordering correct: signatureDelta computed against the OLD baseline (1957) BEFORE the baseline is overwritten (1965); embeddings/observed-merge that run earlier (1909/1922) do not mutate node.signature
- [CLEAN] Desktop wiring — baseline is NOT updated when the diff emit fails (the asked-about bug is absent, in the fail-safe direction): emit (1959/1961) and baseline write (1965) share one try{}; a throw skips 1965 -> baseline freezes -> the same delta re-fires on the next rebuild (duplicate alert, never silence)
- [CLEAN] Desktop wiring — reset on project switch: _prevSigMap=null at electron/main.cjs:271 inside startScanner; ALL load entry points funnel through startScanner (grep: lines 404, 406, 421, 425, 516, 522, 2320, 2834), and invalidateSymbolGraph (150-163) correctly does NOT clear the baseline (it must survive invalidation to diff the next rebuild)
- [CLEAN] Repo's own suite for this area: npx vitest run tests/lang-completeness/flow-sig.test.js -> 5/5 passed on current working tree
- [CLEAN] Exercised CURRENT code: driver prints require.resolve -> F:\MyProg\3dgit\filegraph3d\packages\core\lib\symbol-flow.cjs, fresh node process per run; repo at a937e97, git status shows no tracked-file changes after the audit (only pre-existing untracked build/_* files that were present before I started)

## tarball
캐너리: Two-part. (1) Tarball grep harness: piped planted lines 'package/.codesynapt/observed-edges.json' and 'package/lib/foo.test.cjs' through the exact grep pattern used on the real file list -> both matched (exit 0), proving the clean tarball PASS (exit 1) is a real negative, not a broken pattern. (2) E
- [CLEAN] Tarball hygiene PASS: tar -tzf codesynapt-0.0.4.tgz = 78 files; grep for '.codesynapt|test.cjs|observed-edges|recall-suspects|node_modules|.tgz' over the full list -> 0 hits (grep exit 1)
- [CLEAN] pytracer.py ships PASS: tarball contains package/packages/core/lib/pytracer.py, and installed `cs trace run -- python calc.py` printed 'sys.setprofile attached' and collected '2 raw frame-edges' — the tracer resolves and runs from the installed location
- [CLEAN] Fresh install PASS: npm init -y + npm install ./codesynapt-0.0.4.tgz in F:/tmp/insp-tarball completed (vuln banner aside); bin entries cs/codesynapt/codesynapt-mcp/codesynapt-server present per package.json
- [CLEAN] Installed `cs scan . --summary` PASS: 'root: F:\tmp\insp-sample / files: 3 / edges: 1 / orphans: 1' with correct ext mix (.js 2, .py 1) — Layer-1 edge index.js->src/util.js detected
- [CLEAN] Installed `cs serve` PASS: HTTP API up on 127.0.0.1:17342, /summary returned correct JSON (fileCount 3, edgeCount 1, confidence high:3); coexisted with the user's live backend (kept port lock at 17321, printed explicit CS_PORT targeting note); idle self-exit worked
- [CLEAN] Installed JS symbol layer PASS: `cs symbol find main` -> id index.js#main@2; `cs symbol accounting` -> 'total 5 ... unexplained: 0'; dynamic/declined call sites counted not silenced ('1 declined = 1 stdlib/builtin', '1 dynamic call sites ... recorded')
- [CLEAN] Installed `cs symbol flow main` (Layer-3 JS) PASS: 'L3 helper(literal) / L4 log(literal, call:helper)' — expression layer works from the installed copy
- [CLEAN] Installed `cs trace run -- node index.js` PASS: 'observed call edges: 1 / 1 confirm a static call ... merged into live graph: 1 observed edge — persisted', trace json saved under sample's .codesynapt/traces/
- [CLEAN] Current-code discipline PASS: every command run via `node <abs path to installed bin>` from a fresh node process in F:/tmp/insp-sample; repo comparison used `node <abs repo bin>` against a separately spawned repo serve on port 17343
- [CLEAN] Repo left untouched PASS: npm pack used --pack-destination F:/tmp/insp-tarball (no tgz ever in repo cwd); end-state `git status --porcelain` shows only the same pre-existing untracked build/_*.png|cjs files as before the audit; ports 17342/17343 verified free (netstat empty) and the one lingering serve I started was killed by PID

## win-quirks
캐너리: Two independent canaries, both fired. (1) CRLF/line harness: inserted one real blank line into util.js and rescanned — comparator flipped to 'CANARY GRAPH MATCH (must be false): false' with util.js#helper@3/twice@7 vs baseline @2/@6, proving the line-number comparison can fail. This canary also caug
- [CLEAN] (a) CRLF parity: 3-file project (main.js/util.js/calc.py) scanned as LF, then converted to CRLF with util.js additionally UTF-8 BOM+CRLF (verified via `file`: 'UTF-8 (with BOM) ... CRLF' and od showing 357 273 277 ... \r \n); fresh `cs serve` rescan -> comparator printed 'GRAPH MATCH: true', 'FLOW entry MATCH: true', 'FLOW pymain MATCH: true'; symbol id lists byte-identical: LF and CRLF+BOM both = calc.py#add@1 calc.py#pymain@5 main.js#entry@3 util.js#helper@2 util.js#twice@6 (+3 <module>@1)
- [CLEAN] (b) Spaces in path: project copied to 'F:/tmp/insp win/proj'; `cs scan . --summary` -> 'root: F:\tmp\insp win\proj / files: 3'; `cs symbol flow entry --json` -> 'SPACE-PATH FLOW == LF BASELINE: true'; `cs trace run -- node main.js` (NODE_OPTIONS --cpu-prof-dir under the space path, quoted branch at codesynapt.cjs:2711-2712) -> '1 profile(s) collected', '3 raw frame-edges', 'merged into live graph: 3 observed edges' with entry->helper and entry->twice — identical counts/edges to the no-space control run; no stray .cpuprofile litter outside profDir (find returned nothing)
- [CLEAN] (c) Non-ASCII: 한글테스트.js with function 한글함수(값) called from caller.js — layer-1 `cs deps caller.js` -> 'import 한글테스트.js'; `cs symbol find 한글` -> id '한글테스트.js#한글함수@2' line 2 (correct: line 1 is the Korean comment); `cs symbol node` returned source body 'function 한글함수(값) {\n  return 값 + 1\n}' unmangled; `cs symbol flow callKorean` -> calls 한글함수 at line 4 with arg from 'param:v', return 'from: call:한글함수' — no mojibake anywhere
- [CLEAN] (d) Lease EPERM fallback: code-read registry.cjs:46-73 writeAtomic = temp+rename, retry rename once, fall back to plain fs.writeFileSync overwrite, finally unlink tmp (matches the 2026-06-11 zombie-daemon postmortem comment). Live contention sim: lease file held open by a second process with FileShare ReadWrite (no Delete) -> raw renameSync onto it threw EPERM (contention proven), then registry.touch x5 under the hold: 'BEAT threw= null | lastSeen advanced= true (1781224322995 -> 1781224327168) | beatNo= 4 | tmp litter= []' — reproduced twice (second: 1781224327168 -> 1781224341112, same result)
- [CLEAN] Hygiene: exercised CURRENT repo code only (every run = `node F:/MyProg/3dgit/filegraph3d/packages/core/bin/codesynapt.cjs ...` at a937e97, isolated CS_HOME, CS_PORT pinned to 18431/18207/18433; port 7707 never contacted); all my servers/keepalives/holders killed (netstat shows 18431/18207 clear); repo unchanged: `git status --porcelain | grep -v "^??"` -> 0 lines, remaining entries are pre-existing untracked build/ icon files that predate this audit

## zero-silence
캐너리: Harness includes case 'CANARY deliberately-wrong expectation': trivial `function f(a){ g(a) }` with the assertion deliberately set to expect 'param:b'. Both runs reported 'FAIL CANARY ... expected (deliberately wrong) param:b, got param:a', proving the harness detects mismatches and PASSes are not v
- [CLEAN] JS destructuring `const {x}=o; g(x)` -> unknown AND unresolvedFlows>=1 (case JS-5 PASS)
- [CLEAN] JS ternary arg `g(c?a:b)` -> unknown AND counted (case JS-6 PASS)
- [CLEAN] JS method chaining `return h(a).filter(z)` -> h arg0 param:a, return call:filter (case JS-8 PASS)
- [CLEAN] JS default param `f(a, b=a)` -> b listed as '(pattern)', g(b) unknown+counted, no false claim (case JS-9 PASS)
- [CLEAN] JS nested function isolation: inner fn's calls do NOT leak into outer facts; inner(a) arg correctly param:a (case JS-10 PASS)
- [CLEAN] PY tuple unpacking `a,b = t; g(a)` -> unknown AND counted (case PY-7 PASS)
- [CLEAN] PY kwargs `g(x=a)` -> unknown AND counted, no wrong claim (case PY-8 PASS)
- [CLEAN] PY decorated function: walker finds fn at the def line (Layer-2's startLine convention, verified at symbol-parser-treesitter.cjs:312) and g(a)=param:a (case PY-9 PASS)
- [CLEAN] PY plain reassignment `x=a; x=b; g(x)` -> param:b correct ('assignment' covers rebinding) (case PY-10 PASS)
- [CLEAN] JAVA generics `List<String> r = helper(a); return r;` -> helper arg param:a, return call:helper (case JAVA-2 PASS)
- [CLEAN] JAVA cast `g((String) a)` -> unknown AND counted (case JAVA-3 PASS)
- [CLEAN] CS cast `G((int)a)` -> unknown AND counted (case CS-2 PASS)
- [CLEAN] CS interpolated string `G($"v={a}")` -> unknown AND counted, NOT claimed literal (case CS-3 PASS)
- [CLEAN] CS named argument `G(b: a)` -> param:a correct via argument-node unwrap (case CS-4 PASS)
- [CLEAN] CS plain reassignment `var x=a; x=b; G(x)` -> not param:a (case CS-6 PASS)

