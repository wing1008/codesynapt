PRIORITIZED GAPS (completeness critique of the inspection itself)

1. [BLIND SPOT — 0.0.4 headline delivery channel has ZERO coverage] The CHANGELOG sells signature alerts as "desktop toast + trace-timeline `issue` entry", and the 0.0.4 diff touches `electron/main.cjs` (+60) and `public/app.js` (+19) — but every item tested CLI/HTTP/MCP only. Nobody verified the toast fires, and given sig-alerts CONFIRMED false positives on body-only arrow edits, the toast likely spams users live — unobserved. Same diff also untested: duplicate-desktop warning, the ">30 newly-unreachable mass-flip suppression" (could suppress a REAL mass deletion), and new `/health` epoch/graphVersion/traceVersion fields.
   Check: `npm start` in F:/MyProg/3dgit/filegraph3d; edit a watched file's param list → expect toast+timeline entry; edit only an arrow body → toast must NOT fire (it will, per sig-alerts finding); launch a 2nd desktop on same project → warning; delete 31 files at once → does the suppression hide a real event; `curl http://127.0.0.1:7707/health` → assert epoch/graphVersion/traceVersion present.

2. [BLIND SPOT — Python tracer back half] diff-review only tested capture failure modes (sys.exit, sys.path). Nobody verified the merge/classification pipeline for python edges: confirm/resolve/NEW classification, Windows backslash pytracer paths matching repo fileIds, mtime-expiry guard, persistence across rebuild — and the CHANGELOG claim "survives a crashing script" is directly contradicted by the sys.exit finding (claim itself never tested with `raise`). Also nobody ran `cs trace watch -- python ...` (BACKLOG marks py watch deferred — what does it do today, clean error or garbage?).
   Check: multi-edge py fixture without sys.exit → `node packages/core/bin/codesynapt.cjs trace run -- python app.py`, then `cs symbol summary` + GET /symbol/... to confirm edges classified+persisted; rerun after `git stash`-style file edit to test mtime expiry; `cs trace run` on a script ending in `raise RuntimeError` (CHANGELOG claim); `cs trace watch -- python app.py`.

3. [BLIND SPOT — TypeScript/TSX flow never exercised] `packages/core/lib/symbol-flow.cjs:237` routes ts/tsx/jsx into the babel walker; all flow-real/zero-silence/e2-blast fixtures were plain JS or py/java/cs. TS param type annotations, `b?: number`, defaults-with-types, generics, and `constructor(private x)` may break parse or yield phantom params — and `signatureDelta` on typed signatures (type-only change = alert or not?) is untested.
   Check: fixture `f.ts` with `function f(a: string, b: number = 5): void { g(a) }` → `cs symbol flow f` and `cs symbol flow f a`; grep `parserOpts|plugins` in `packages/core/lib/symbol-flow.cjs` for a `typescript` babel plugin; type-only edit → signature delta?

4. [CLEAN CLAIM NOT TRUSTABLE — blockers=0 findings contradicts tarball/journey HIGHs] "Layer-2 silently dead in EVERY npm install" + "0.0.4 headline python trace breaks multi-file projects" are plausibly release-blocking, yet `blockers` reported clean with 10 checks. Either its checklist predates/excludes the 0.0.4 diff or it under-covered. Likewise burn-spots (0 findings, only 6 cleanChecks) is adjacent to tarball, which DID find a HIGH — confirm burn-spots' grep harness even could have caught a wasm-path-resolution bug (it's a runtime path issue, not tarball content — likely structurally out of its scope).
   Check: re-derive blockers' checklist FROM the v0.0.3..HEAD diff stat (pytracer.py, symbol-flow*.cjs, electron/main.cjs, control-server.cjs) and assert each touched surface has ≥1 blocking-grade check; ask whether the tarball wasm HIGH appears in blockers' worklist at all.

5. [BLIND SPOT — deliberate false-negative path in sig alerts] CHANGELOG: "colliding same-named object methods are excluded rather than false-flagged" — an intentional suppression nobody probed (change a param on one of two same-named methods → alert silently swallowed?). Also `collectSignatures`/`signatureDelta` run over ALL graph symbols, but every sig-alerts finding is JS; py/java/cs signature strings (typed params, py defaults) untested for false pos/neg.
   Check: fixture with two classes each defining `render(a)`; add a param to one → does any delta surface; repeat param-add in a `.py` and `.cs` fixture → delta expected.

6. [BLIND SPOT — Lua fix unverified] 0.0.4 CHANGELOG claims "Lua bare calls were all misclassified as member calls" is fixed; the diff shows only +2 lines in misc-langs.test.js and NO inspection item touched Lua.
   Check: lua fixture `local function a() end; local function b() a() end` → `cs symbol summary` must show the call edge and the zero-silence ledger must not be mute; bare call to undefined name must hit ledger, not builtin guard.

7. [FAKE/MISMATCHED CANARIES — discount perf and docs cleanChecks] perf's canary (deleted an import, edge-count comparator fired) proves the GRAPH harness can fail, not the PERF harness — there is no timing canary, so all 7 perf cleanChecks are unfalsified. docs' canary (`cs nonexistentcmd` exits 1) proves the CLI errors, not that the docs-vs-behavior comparator can flag a wrong doc claim.
   Check (perf): inject a 3s sleep into the scan path of a scratch copy, rerun the perf harness — it must flag a regression, else its PASSes mean nothing. Check (docs): take one VERIFIED-true README claim, corrupt it in the harness's expected-set, rerun — must flip to FAIL.

8. [UNDER-COVERAGE — win-quirks cleanChecks=5 vs a path-sensitive 0.0.4 diff] pytracer's in-repo filter and runpy are filename-matching code on Windows, but only CRLF and port-lock were checked. Untested: project under a Korean/Unicode path (the user's actual environment), spaces in path, drive-letter case mismatch (F:\ vs f:\) defeating the in-repo frame filter, and `py.exe` launcher beyond the already-found `-m` arg bug.
   Check: copy a 2-file py fixture to `F:\tmp\한글 테스트\`, run `cs trace run -- python app.py` there and assert observed edges > 0; repeat from a lowercase-drive cwd (`cd f:\tmp\...`).